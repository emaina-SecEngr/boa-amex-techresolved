FROM python:3.12-slim
RUN groupadd -r lbb && useradd -r -g lbb lbb
RUN pip install --no-cache-dir fastapi uvicorn
WORKDIR /app
COPY health_wrapper.py main.py
USER lbb
EXPOSE 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
